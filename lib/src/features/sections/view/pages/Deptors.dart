import 'package:flutter/material.dart';

class DeptorsPages extends StatelessWidget {
  const DeptorsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
