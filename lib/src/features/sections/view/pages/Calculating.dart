import 'package:flutter/material.dart';

class CalculatingPages extends StatelessWidget {
  const CalculatingPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
